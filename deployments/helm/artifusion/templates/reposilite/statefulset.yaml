{{- if .Values.reposilite.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "artifusion.reposilite.serviceName" . }}
  labels:
    {{- include "artifusion.reposilite.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "artifusion.reposilite.serviceName" . }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "artifusion.reposilite.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/reposilite/configmap.yaml") . | sha256sum }}
        {{- if .Values.artifusion.config.github.required_org }}
        checksum/github-packages-secret: {{ include (print $.Template.BasePath "/reposilite/secret.yaml") . | sha256sum }}
        {{- end }}
      labels:
        {{- include "artifusion.reposilite.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- include "artifusion.imagePullSecrets" . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "artifusion.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.reposilite.podSecurityContext | nindent 8 }}
      initContainers:
        - name: copy-config
          image: cgr.dev/chainguard/busybox:latest
          command:
            - sh
            - -c
            - |
              mkdir -p /app/data
              mkdir -p /shared
              rm -f /app/data/configuration.shared.json /shared/configuration.shared.json
              cp /config/configuration.cdn /app/data/configuration.cdn
              cp /config/configuration.shared.template.json /shared/configuration.shared.json
              {{- if .Values.artifusion.config.github.required_org }}
              escape_sed() {
                printf '%s' "$1" | sed 's/[\/&]/\\&/g'
              }
              sed -i "s/__GITHUB_PACKAGES_USERNAME__/$(escape_sed "$GITHUB_PACKAGES_USERNAME")/g" /shared/configuration.shared.json
              sed -i "s/__GITHUB_PACKAGES_TOKEN__/$(escape_sed "$GITHUB_PACKAGES_TOKEN")/g" /shared/configuration.shared.json
              {{- end }}
              echo "Local configuration copied to /app/data/configuration.cdn"
              echo "Shared configuration rendered to /shared/configuration.shared.json"
          {{- if .Values.artifusion.config.github.required_org }}
          env:
            - name: GITHUB_PACKAGES_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "artifusion.reposilite.serviceName" . }}-secret
                  key: github-username
            - name: GITHUB_PACKAGES_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "artifusion.reposilite.serviceName" . }}-secret
                  key: github-token
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: data
              mountPath: /app/data
            - name: shared-config
              mountPath: /shared
      containers:
        - name: reposilite
          securityContext:
            {{- toYaml .Values.reposilite.securityContext | nindent 12 }}
          image: {{ include "artifusion.reposilite.image" . }}
          imagePullPolicy: {{ .Values.reposilite.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: REPOSILITE_ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "artifusion.reposilite.serviceName" . }}-admin
                  key: admin-token
            - name: REPOSILITE_OPTS
              value: "--local-configuration=/app/data/configuration.cdn --shared-configuration=/shared/configuration.shared.json --token admin:$(REPOSILITE_ADMIN_TOKEN)"
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            {{- toYaml .Values.reposilite.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /app/data
            - name: shared-config
              mountPath: /shared
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: {{ include "artifusion.reposilite.serviceName" . }}
        - name: shared-config
          emptyDir: {}
  volumeClaimTemplates:
    - metadata:
        name: data
        annotations:
          helm.sh/resource-policy: keep
      spec:
        accessModes: [ {{ .Values.reposilite.storage.accessMode | quote }} ]
        {{- if .Values.reposilite.storage.storageClass }}
        storageClassName: {{ .Values.reposilite.storage.storageClass | quote }}
        {{- else if .Values.global.storageClass }}
        storageClassName: {{ .Values.global.storageClass | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.reposilite.storage.size }}
{{- end }}
